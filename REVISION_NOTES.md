# Revision notes

The first Project Koios commits predate the in-message revision-note convention:

- `7189273` — Established the Python 3.14 build and core concurrent colored-net regression baseline.
- `503372b` — Added Python 3.14 `ast.Constant` support for expression substitution.
- `99c2d68` — Verified frozen, slotted, runtime-typed workflow tokens through concurrent branches and a join.

Subsequent Project Koios commits carry a concise `Revision note:` paragraph in the commit message body.
